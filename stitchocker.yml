sets:
  default:
    - @services
    - @sites

  services:
    - @services/reverse-proxy
    - @services/mysql
    - @services/redis
    - @services/memcached

  sites:
    - testing
    - rabbitmq
  
